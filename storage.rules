rules_version = '2';

service firebase.storage {
  match /b/{bucket}/o {
    match /uploads/{allPaths=**} {
      // Public read access for all files in the uploads folder
      allow read: if true;
      
      // Allow writes only for authenticated users
      allow write: if request.auth != null;
    }
  }
}
